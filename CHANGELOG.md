# 更新记录 · Changelog

本文件记录面向用户的版本变化。当前协作状态见 [`COLLABORATION.md`](COLLABORATION.md)，长历史归档在 `docs/history/`。

格式参考 [Keep a Changelog](https://keepachangelog.com/)，版本号为产品内部迭代号（非 npm 语义化版本）。

## [Unreleased]

- 新增 GitHub Pages 项目展示页：`/showcase/`。它作为给 GitHub 访客看的项目主页，使用现有 demo 视频和截图介绍 Direct Light；线上 demo 根路径仍保持为可直接使用的应用本体。
- Vite 生产构建现在会同时产出应用根页和展示页；Tauri 构建仍只打包应用根页，避免桌面包额外带展示页素材。

## [1.0.3] - 2026-06-25 · 修复：阴影漏光（法线偏移 + PCF 柔和阴影）

> 通过用户验收并已发布；TopBar / `package.json` / Tauri / Cargo / Cargo.lock 均为 `1.0.3`，tag `v1.0.3`。

- **给哲学家们擦掉了身上的漏光。** 强光 / 直射下，VSM 阴影会在曲面（尤其人像剪影）上渗出一圈漏光（light bleeding），现在可控了。
- **每盏灯新增「法线偏移」滑杆**（灯光面板，0–0.05）：沿表面法线偏移阴影采样以消除曲面漏光，代价是几乎看不出的阴影角度偏移；强光 / 直射场景最明显。
- **新增「柔和阴影（PCF）」开关**（白棚面板 → 渲染）：极端布光下 VSM 救不回来时的兜底，彻底消除人像与背景上的漏光，代价是半影不再丝滑、表面略平。默认仍是 VSM + 法线偏移（观感更好）；开 PCF 后法线偏移依然可调。
- 集成收口（Claude）：运行时切换阴影算法时，强制重编接收阴影的材质——three.js 把 `SHADOWMAP_TYPE_*` 烘在材质 shader 的编译期，只切 `shadowMap.type` 不会让表面改用新算法采样。顺带修掉原 PR 在 `tsc -b` / `eslint` 下的两处报错（`vite dev` 看不出，故贡献者本地未暴露），lint / build 全绿。
- 再次特别感谢斯坦福的 Dr. Zhang（[@zczam](https://github.com/zczam)，PR #2）——上次是请来哲学人物，这次是给白棚除漏光。

## [1.0.2] - 2026-06-25 · 用户自定义人像模型 + 更多哲学人物

> 通过用户验收并已发布；TopBar / `package.json` / Tauri / Cargo / Cargo.lock 均为 `1.0.2`，tag `v1.0.2`。

- **在博士的建议下添加了更多哲学人物。**
- **用户自定义人像模型**：把 `.glb` 文件放进 `src/models/`，它会自动出现在人物面板「外观 → 用户模型」列表里（构建期 Vite glob 自动发现，运行期按包围盒自动缩放、落地，无需逐模型配置）。比起调假人滑块，导入成品模型在高面数角色上更省事。
- **随附两个新人像**：「哲学家」与「哲学家 (胸像)」，给原本朴素的白棚加点角色。
- 默认人物仍是程序化**假人**，自定义模型为可选项；选中自定义模型时按需懒加载（加载中以假人占位），姿态滑块自动禁用（成品模型不走那套关节滑块）。
- 特别感谢斯坦福的 Dr. Zhang（[@zczam](https://github.com/zczam)），让无聊的白棚多了一些哲学和地牢的气息。

## [1.0.1] - 2026-06-24 · 修复：自由拖动受白棚尺寸约束

> 通过用户验收并已发布；TopBar / `package.json` / Tauri / Cargo / Cargo.lock 均为 `1.0.1`，tag `v1.0.1`。

- 修复自由拖动越界：在画布里自由拖动**灯光 / 相机 / 人物 / 道具**时，落点现在按白棚实际 `width` × `depth` 夹紧（四面封边，含敞开的前方 +Z，留 0.3m 余量），不再沿用写死的 ±20m 范围。此前灯光 / 人物 / 道具完全不受白棚尺寸约束，相机前方也能拖出棚外。白棚尺寸改动后拖动边界实时跟随。新增纯函数 `src/domain/studioBounds.ts` 的 `clampToStudioFootprint`，由 `GroundDragController` 读当前 `studio` 调用。

## [1.0.0] - 2026-06-24 · 多语言 UI · 首个正式大版本

- 新增 [`V0_10_I18N_SPEC.md`](V0_10_I18N_SPEC.md)，定稿多语言口径：语言是 app preference，不进入场景、保存方案、A/B 或自定义器械数据。
- 首批语言定为简体中文 / 英语 / 日语；默认仍是简体中文。
- 约定实现方式：轻量 typed in-repo i18n，不先引入重型依赖；v0.10a-d 拆分为基础设施、核心 UI 抽取、内置显示标签/copy pass、验收收口。
- **i18n 基础设施已落地并通过用户验收**：新增 `src/i18n/*`（`languages`/`t()`/`useT` + 按域拆分的 `messages/`），store 顶层 `language` + `setLanguage`，独立持久化 key `direct-light.language.v1`，顶栏语言菜单（中/英/日）。
- **核心 UI 抽取（tier-A）已全部落地并通过用户验收**：顶栏、视角徽标、对象列表、5 个参数面板（人物/灯/物体/白棚/灯具子区）、方案栏、A/B 对比视图全部走 `t()`。
- **内置显示标签 + 派生文案已收口**：灯型、器械能力、内置器械、控光附件、目标模式、颜色、机位、姿态、道具/材质/类型、承载面、有效光质摘要、导演灯光简介、A/B `sceneDiff` 和 debug preset title 均通过 id/helper 本地化；源数据表和用户输入不被改写。
- 用户真机视觉验收 + Codex en/ja copy 复核均已通过（2026-06-24）。多语言 UI 全线完成，作为**首个正式大版本 `v1.0.0`** 发布（网页 + macOS 桌面）；TopBar / `package.json` / Tauri / Cargo 版本同步到 `1.0.0`。

## [0.9.0] - 2026-06-23 · 自定义灯具导入 / 导出

> 通过用户视觉验收并已发布；TopBar / `package.json` / Tauri / Cargo 均为 `0.9.0`。详见 [`V0_9_CUSTOM_FIXTURE_SPEC.md`](V0_9_CUSTOM_FIXTURE_SPEC.md)。

- 自定义灯具器械：与内置器械同形，可进同一「器械」下拉、走同一套 `applyFixturePreset`。
- 把当前调好的灯一键存成自定义器械；自定义器械本地保存（localStorage）。
- 自定义器械 JSON 文件导入 / 导出（容错校验：非法值钳制并提示、坏条目跳过、永不崩）。
- 器械下拉分「内置器械 / 我的器械」，选中自定义器械显示「自定义」徽标，可删除。
- 自定义器械为本地保存（localStorage `direct-light.customFixtures.v1`）；跨设备 / 分享靠「导出 / 导入」JSON 手动搬运。
- （实现拆分：v0.9a 纯函数 + 数据结构、v0.9b 文件导入导出 / 校验、v0.9c 下拉 / store / 持久化、v0.9d 携带验证 + 版本号 + 文档收口；内置器械行为零变化。）

## [0.8.0] - 2026-06-22 · 多光源 / 多灯管理

- 灯光上限从 3 提高到 6，默认场景仍保持 Key / Fill / Rim 三盏。
- 新增第 4-6 盏灯的确定性初始位置，避免新增灯堆在同一个点。
- 左侧灯光列表显示 `灯光 N/6`，满额后显示 `满 6`，复制按钮在满额时禁用。
- A/B 摘要、保存 / 加载 / 复制方案和导出路径确认不裁剪 6 灯状态。
- 修复同轮两个交互问题：人物拖拽会被夹在白棚地面范围内；自由视角横向触控滚动用于左右转，纵向滚动仍用于缩放。
- 完成 v0.8 后，下一步主线调整为 v0.9 自定义灯具预设 JSON 导入 / 导出；多语言在 v0.10 另行规格化。

## [0.7.1] - 2026-06-21 · 应用图标

- 新增正式 App 图标（影棚 + 人物 + 紫色光圈），替换 Tauri 默认占位 logo；由 `app-icon.png`（1024×1024）经 `npx tauri icon` 生成全套 macOS 图标。
- 顶栏版本号 `v0.7.0 → v0.7.1`；`package.json` / `tauri.conf.json` / `Cargo.toml` 版本同步到 `0.7.1`。
- 移除 `tauri icon` 顺带生成的 iOS / Android 图标（本项目只发 macOS 桌面）。

## [0.7.0] - 2026-06-21 · 可开源第一版

可开源第一版收口。功能与 v0.6e 一致，新增开源工程化收口：

- 新增 `LICENSE`（MIT）、`CONTRIBUTING.md`、`CHANGELOG.md`、英文 `README.en.md`。
- `package.json` 补齐开源元数据（描述、license、author、repository、keywords、`engines`），版本号 `0.7.0`。
- README 顶部新增「快速开始 / 项目结构 / 已知限制 / 许可证」开源门面，原产品需求文档保留在下方。
- 清理仓库杂物，规范 `.gitignore`，初始化 git 仓库并开源到公开仓库。
- 新增 GitHub Actions 自动部署：每次 push 到 `main` 自动构建并发布在线 demo —— https://oukeming64-tech.github.io/direct-light/ （`vite.config.ts` 生产构建使用 `base: '/direct-light/'`，开发仍在根路径）。
- 新增 macOS 桌面封包（[Tauri](https://tauri.app/)）：`src-tauri/` 脚手架 + `build:tauri` 脚本（`--mode tauri` → 相对 `base`）+ `.github/workflows/release.yml`（push `v*` tag → macOS runner 构建通用包 → 挂到 Releases 草稿）。桌面版与网页版同一套前端代码。
- 顶栏版本号 `v0.6e → v0.7.0`。

当时已知限制：最多 3 盏灯、界面语言只有简体中文、桌面优先、渲染为沟通向近似（见 README）。

## [0.6.0] - 2026-06-21 · 控光线（v0.6a–v0.6e）

灯上控光附件与棚内独立控光器材全线完成。

- **v0.6a** 灯上附件 MVP：中号柔光箱 / 蜂巢 / 标准反光罩 / 柔光布，通过 `modifierId` 影响有效亮度、光束、柔硬和彩色溢光，不破坏原始灯光参数。
- **v0.6b** 附件 3D 可见形体 + 镜头视角「导演简介」状态条（如 `Key Light · Nanlux Evoke 600C · 中号柔光箱 · 柔光主光`）。
- **v0.6c** 棚内独立控光器材作为场景对象：黑旗 / 反光板 / 柔光布框，可拖动、旋转、改尺寸（本阶段零光学）。
- **v0.6d** 控光器材近似光学：黑旗吃光 / 负补光 / 简化遮挡、反光板暗部弱补光、柔光布框在灯人之间软化；运行时派生，不写回灯光或器材数据。
- **v0.6e** 收口：A/B 差异摘要把「控光器材」从「道具」拆出；保存 / 加载 / 复制 / A-B 不丢附件与派生光学；导出图保留器材视觉。

## [0.5.0] - 2026-06-20 · 灯具预设库 + 渲染可信度

- **v0.5.0** 8 个灯具器械预设（COB 白 / 双色温、Nanlux Evoke 600C、LED 面板、RGB 灯管、菲涅尔、小型效果灯、暖色实景灯）；面板「器械」下拉一键套用默认光质，仍可手动微调；新增能力标签（全彩 / 双色温 / 暖色 / 白光）。
- **v0.5.1** 渲染可信度：一体化无缝弧形背景、可见灯体、地面反射进入填光与彩色反弹公式。

## [0.4.0] - 2026-06-19 · 人物姿态 + 摄影机控制

- **v0.4a–b** 层级骨架假人、9 个姿态预设、关键关节滑杆、坐姿与承载面坐/站分流、前臂内旋。
- **v0.4.5** attach-to-support：人物绑定到桌椅 / 小舞台，承载物移动旋转时人物跟随。
- **v0.4.6** A/B 对比产品引导：差异摘要、B 冻结来源/时间、空状态三动作。
- **v0.4.7** 摄影机控制：方位角 / 距离 / 高度 / 看向高度 / 焦段 / 画幅、5 个机位预设、从自由视角取景、目标锁定（手动 / 锁定人物 / 多人中心）、棚内夹紧。
- **v0.4.8** `store.ts` 无行为变更拆分（685 → 38 行，八组 action 工厂）。

## [0.3.0] - 2026-06-18 · 白棚结构 / 道具 / 人台

- 道具与结构对象库：长桌 / 圆桌 / 椅子 / 凳子 / 沙发 / 台座 / 服装人台 / 背景板 / 直播圆形小舞台等，可拖动、旋转、复制、删除、改材质。
- 人物离地高度与一键放到承载物（桌面 / 椅面 / 台座 / 小舞台）。
- 灯光目标模式：手动 / 锁定人物 / 多人中心。

## [0.2.0] - 2026-06-18 · 多人物与站位

- 添加 / 复制 / 删除 / 选择 / 重命名人物（上限 5），多人投影与互相遮挡，俯视站位编辑，默认 Actor A–E。

## [0.1.0] - 2026-06-17 · 首个可用原型

- 白棚空间、简化站立人物、最多 3 盏可调灯（位置 / 高度 / 距离 / 角度 / 亮度 / 颜色 / 色温 / 光束 / 柔硬）、硬光 / 柔光 / 面光、镜头视角与俯视图、方案保存到 localStorage、导出预览图、A/B 对比。
